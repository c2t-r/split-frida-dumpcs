# split frida dumpcs

split dump.cs, generated by `frida-il2cpp-bridge` to multiple files  
this tool helps to manage dump.cs by git

## usage

[input] [output dir]

```bash
uv run main.py "../dump.cs" "dump"
```
